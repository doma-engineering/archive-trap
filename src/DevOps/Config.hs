-- | A fistful of functions for putting server configurations on Git
--module DevOps.Config (snapConfigs, applyConfigs, snapSys, applySys) where
module DevOps.Config where

import Turtle
import Turtle.Directory.Filter
import Prelude hiding (FilePath)

{--
snapConfigs :: Greenlist -> Redlist -> IO [FilePath]
snapConfigs = undefined

snapSys :: OS -> IO Snapshot
snapSys = undefined

applySys :: Snapshot -> IO (Maybe ())
applySys = undefined
--}
